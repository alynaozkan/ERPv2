@model IEnumerable<ERP.Models.Kontrol.Genel.BSMGRNNMGEN002>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LANTEXT)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FCOMCODE)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LANTEXT)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FCOMCODE.COMCODE)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LANCODE">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LANCODE">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LANCODE">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
