@model IEnumerable<ERP.Models.Kontrol.Genel.BSMGRNNMGEN004>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CITYTEXT)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.COUNTRY)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CITYTEXT)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.COUNTRY.COUNTRYCODE)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CITYCODE">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CITYCODE">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CITYCODE">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
